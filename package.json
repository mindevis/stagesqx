{
  "name": "stagesqx-release",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "release": "cross-env CI=false semantic-release --no-ci",
    "release:dry": "cross-env CI=false semantic-release --dry-run"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^11.0.0",
    "cross-env": "^7.0.3",
    "semantic-release": "^24.2.3"
  }
}
